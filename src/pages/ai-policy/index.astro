---
import PolicyHeroSection from '@/shared/ui/templates/policy-hero-section';
import { aiPolicyContent } from './constant';
import PresentationLayout from '@/layouts/PresentationLayout.astro';
const { title = 'AI Policy', description = 'Thought Metrics - AI Policy' } =
  Astro.props;
---

<PresentationLayout title={title} description={description}>
  <PolicyHeroSection
    client:load
    content={aiPolicyContent.content}
    head={title}
  />
</PresentationLayout>
